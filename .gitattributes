# 统一行尾：仓库内一律 LF（避免 Windows/macOS/Linux 协作时整文件抖动）
* text=auto eol=lf

# 二进制素材不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary

# 构建产物：标为 generated，GitHub 折叠 diff
lib/client.js linguist-generated=true
